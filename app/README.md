# Chicas Map — Grok app source

Snapshot of the Grok-built web app (home, satellite map, sales list, free listing form).

This folder is **source only**. It does **not** replace the live GitHub Pages site in `webapp/`.

Brand images already live at `webapp/images/`. Sale feed data lives at `webapp/data/cities/san-antonio.json`.
