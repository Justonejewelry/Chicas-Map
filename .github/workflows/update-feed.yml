# RETIRED 2026-08-13
# Feed rebuild is now part of Chica Daily discovery + orchestrator.
# Do not re-enable the schedule.

name: "[retired] Update Chicas-Map feed"

on:
  workflow_dispatch:
    inputs:
      note:
        description: "This workflow is retired. Use Chica Daily instead."
        required: false
        default: "retired"

permissions: {}

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: Retired notice
        run: |
          echo "This workflow was retired. Use Actions → Chica Daily (Master Workflow)."
          exit 0
