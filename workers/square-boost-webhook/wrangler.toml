name = "chicas-square-boost-webhook"
main = "src/index.js"
compatibility_date = "2024-11-01"

# Non-secret defaults (override in dashboard if needed)
[vars]
BOOST_AMOUNT_CENTS = "900"
BOOST_CURRENCY = "USD"
GITHUB_REPO = "Justonejewelry/Chicas-Map"
NOTIFY_EMAIL_TO = "mr.jsciaraffa@gmail.com"

# Secrets via `wrangler secret put`:
#   SQUARE_SIGNATURE_KEY
#   SQUARE_NOTIFICATION_URL
#   NOTIFY_FORMSPREE_ID
#   GITHUB_TOKEN  (optional)
