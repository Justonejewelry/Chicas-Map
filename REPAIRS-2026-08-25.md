# Repairs applied 2026-08-25 (audit follow-up)

## app/src/lib/sales.ts
- Runtime `isValidSale` / coord guards on feed + localStorage
- Cap local submissions (50)
- Quota-safe `saveSubmission`
- Hardened `mapsLinks` (finite coords, address length cap)
- `fetchFeed` filters invalid pins and uses no-cache

## app/src/components/sale-map.tsx
- Map init: try/catch, ResizeObserver, full teardown (clearLayers + remove)
- Satellite toggle: cancelled flag + ready guard
- Marker rebuild: skip non-finite coords, use selectedIdRef, title length cap
- nearMe: error logging, timeout 10s, maximumAge, finite check
- External map links: `rel="noopener noreferrer"`

## app/src/routes/submit.tsx
- Field length clamps + min length checks
- Type allow-list
- Nominatim: proper User-Agent, AbortSignal.timeout(10s), SA bounding-box soft filter
- Unique-ish tip IDs
- Comment hook for future moderated Worker

## webapp/_headers
- Content-Security-Policy (tiles, fonts, nominatim, workers)
- Cross-Origin-Opener-Policy
- `/data/*` short cache + SWR

## workers/tips-submit/README.md
- Scaffold for moderated tip endpoint (rate-limit + queue)

## Not done in this pass (larger follow-ups)
- Full MapLibre migration + clustering
- City-parameterized routes (`/:city/map`)
- Live Worker implementation + GitHub tip workflow wiring
